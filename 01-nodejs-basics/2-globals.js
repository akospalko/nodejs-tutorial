console.log('+__dirname: it executes dirname of the program \n result: ', __dirname);
console.log('+__filename: it executes filename of the program \n result: ', __filename);
// console.log('+require: it is a func. to use modules (commonjs) \n result: ', require);
console.log('+require: it is a func. to use modules (commonjs) \n result: ', module);
//console.log('+process: gives info about the env where the program gets esxecuted: ', process);