//create a function that adds numbers together -> 
//execute function here -> 
//require it in 'main'

const n1 = 10;
const n2 = 12;

function addNums(number1, number2) {
  console.log(`the sum of ${n1} and ${n2} is ${n1 + n2}`);
}

addNums(n1,n2);